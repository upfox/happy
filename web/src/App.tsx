import React from 'react';

import './styles/global.css';

import Routes from './routes';


function App(){
  return (
    <Routes /> 
  );
}

export default App;

// yarn | npx create-react-app web (nome do projeto) --template typescript

// Passando atributo para componente, chamamos de propriedade